@{
    ViewBag.Title = "Show";
    Layout = "~/Views/Shared/_ParentPage1.cshtml";
}
<style>
    .bar {
        width: 70%;
        height: 500px;
        margin: 0 auto;
    }
</style>
<div class="layui-tab layui-tab-card bar" lay-filter="test">
    <ul class="layui-tab-title">
        <li class="layui-this">商务标准间</li>
        <li>温馨大床房</li>
        <li>情侣房</li>
        <li>普通单人间</li>
        <li>海洋主题套房</li>
        <li>樱花主题套房</li>
        <li>豪华客房</li>
    </ul>
    <div class="layui-tab-content" style="height: 100px;">
        <div class="layui-tab-item">
            <table class="layui-table tablepage" lay-data="{url:'/Rooms/Get?state=商务标准间', page:true, id:'1'}" lay-filter="demo">
                <thead>
                    <tr>
                        <th lay-data="{align:'center',field:'Room_Num'}">房间号</th>
                        <th lay-data="{align:'center',field:'Room_State',templet:'#state'}">状态</th>
                    </tr>
                </thead>
            </table>
        </div>
        <div class="layui-tab-item">
            <table class="layui-table tablepage" lay-data="{url:'/rooms/get?state=温馨大床房', page:true, id:'2'}" lay-filter="demo">
                <thead>
                    <tr>
                        <th lay-data="{align:'center',field:'Room_Num'}">房间号</th>
                        <th lay-data="{align:'center',field:'Room_State',templet:'#state'}">状态</th>
                    </tr>
                </thead>
            </table>
        </div>
        <div class="layui-tab-item">
            <table class="layui-table tablepage" lay-data="{url:'/rooms/get?state=情侣房', page:true, id:'3'}" lay-filter="demo">
                <thead>
                    <tr>
                        <th lay-data="{align:'center',field:'Room_Num'}">房间号</th>
                        <th lay-data="{align:'center',field:'Room_State',templet:'#state'}">状态</th>
                    </tr>
                </thead>
            </table>
        </div>
        <div class="layui-tab-item">
            <table class="layui-table tablepage" lay-data="{url:'/rooms/get?state=普通单人间', page:true, id:'4'}" lay-filter="demo">
                <thead>
                    <tr>
                        <th lay-data="{align:'center',field:'Room_Num'}">房间号</th>
                        <th lay-data="{align:'center',field:'Room_State',templet:'#state'}">状态</th>
                    </tr>
                </thead>
            </table>
        </div>
        <div class="layui-tab-item">
            <table class="layui-table tablepage" lay-data="{url:'/rooms/get?state=海洋主题套房', page:true, id:'5'}" lay-filter="demo">
                <thead>
                    <tr>
                        <th lay-data="{align:'center',field:'Room_Num'}">房间号</th>
                        <th lay-data="{align:'center',field:'Room_State',templet:'#state'}">状态</th>
                    </tr>
                </thead>
            </table>
        </div>
        <div class="layui-tab-item">
            <table class="layui-table tablepage" lay-data="{url:'/rooms/get?state=樱花主题套房', page:true, id:'6'}" lay-filter="demo">
                <thead>
                    <tr>
                        <th lay-data="{align:'center',field:'Room_Num'}">房间号</th>
                        <th lay-data="{align:'center',field:'Room_State',templet:'#state'}">状态</th>
                    </tr>
                </thead>
            </table>
        </div>
        <div class="layui-tab-item">
            <table class="layui-table tablepage" lay-data="{url:'/rooms/get?state=豪华客房', page:true, id:'7'}" lay-filter="demo">
                <thead>
                    <tr>
                        <th lay-data="{align:'center',field:'Room_Num'}">房间号</th>
                        <th lay-data="{align:'center',field:'Room_State',templet:'#state'}">状态</th>
                    </tr>
                </thead>
            </table>
        </div>
    </div>
</div>
<script type="text/html" id="state">
    {{#  if(d.Room_State === 1){ }}
    {{ '已预订'}}
    {{#  } else if(d.Room_State === 2){ }}
    {{ '空房' }}
    {{#  } else if(d.Room_State === 3){ }}
    {{ '已入住'}}
    {{# }else{ }}
    {{ '已退房'}}
    {{# } }}
</script>
<link href="~/layui-v2.4.5/layui/css/layui.css" rel="stylesheet" />
<script src="~/layui-v2.4.5/layui/layui.all.js"></script>
<script>
    $(function () {
        layui.use(['element', 'table'], function () {
            var table = layui.table;
            var $ = layui.jquery
                , element = layui.element; //Tab的切换功能，切换事件监听等，需要依赖element模块
            element.on('tab(test)', function (data) {
                //var src=$(".layui-tab-item.layui-show").find("iframe").attr("src");
                //$(".layui-tab-item.layui-show").find("iframe").attr("src",src);
            });
        });
    })

 
</script>